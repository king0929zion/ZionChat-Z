## v0.8.0

- 按 ZionChat 继续精确回归聊天输入区：默认输入胶囊高度改回与左侧工具按钮一致，发送按钮更贴近右下角，工具面板统一改成黑白中性按钮与 Zion 图标入口。
- 修复设置链路的头部遮罩过重问题，收轻浮动顶栏毛玻璃覆盖范围，并同步统一设置主页的分组卡片、入口行高度、圆角、按压态和黑白按钮风格。
- 默认模型设置页整页改成更接近 ZionChat 的分节选择器结构，支持同风格的模型选择底部弹层与高级 Prompt 设置面板。
- 应用图标已替换为新的手写 `z` 版本，并同步更新普通图标、自适应前景图标和单色主题图标资源。

## v0.7.0

- 继续按 ZionChat 收口聊天与设置视觉：补强顶部胶囊导航、聊天输入区、侧边栏和主要设置链路的圆角、配色、按压反馈与 Zion 图标使用。
- 修复设置链路的结构问题：收紧浮动头部与内容区的安全间距，并补齐显示、搜索、TTS、MCP、文件、关于等子页的可见内容与统一卡片外观。
- 应用名称与图标同步更新为 `ZionChat Z`，关于页链接切到新仓库 `king0929zion/ZionChat-Z`。
- 修复 `0.6.0` 首轮自动构建暴露的 `SettingWebPage` 缺失 `Alignment` 引用问题，恢复签名 APK 编译。

## v0.5.0

- 修复 `0.4.0` 首轮构建暴露的 Compose 编译问题，补齐聊天页、输入区、侧边栏和统一顶栏改造中的权重作用域、头像类型、字符串资源调用与自定义图标路径 API 兼容性。
- 延续 ZionChat 聊天壳层重构：顶部保持单胶囊导航，输入区保持“工具按钮 + 胶囊输入框”结构，侧边栏保持 Zion 式搜索/菜单/会话/资料卡布局。
- 统一顶栏容器继续上调安全顶距，进一步缓解设置链路在新顶栏样式下的遮挡风险。

## v0.4.0

- 聊天页顶部改成更贴近 ZionChat 的单层导航：左侧汉堡菜单、中间单胶囊标签、右侧新建按钮，去掉原先双行标题和外露工具按钮。
- 输入区进一步严格按 ZionChat 收口为“单工具按钮 + 单胶囊输入框”结构，模型选择、搜索、推理预算、MCP、预览模式与快捷消息全部收进工具面板统一管理。
- 侧边栏改成 ZionChat 式结构：顶部搜索入口与新建按钮、单列功能菜单、会话列表、底部个人资料卡，并替换这条链路上的主要图标为 Zion 风格。
- 上调统一浮动头部的内容安全顶距，缓解设置链路在新 Zion 顶栏下的内容遮挡问题。

## v0.3.0

- 修复真机闪退：补上 `libsimple.so` 的运行时兜底解压与 SQLite 扩展路径修复，解决新版包在部分设备上因原生库未落地到 `nativeLibraryDir` 导致的启动后点击即崩溃问题。
- 聊天页顶部栏进一步向 ZionChat 收口，统一菜单、返回、新建会话和工具关闭按钮的图标风格与标题字号。
- 聊天输入区改成更接近 ZionChat 的胶囊式布局，把发送按钮内收进输入容器，并替换为 ZionChat 风格的发送、加号、关闭、相册、相机、文件、思考等关键图标。

## v0.2.0

- 应用安装包名已改为 `io.github.king0929zion.zionchatz`，后续签名构建、Release 附件与新安装包都会使用这个新包名。
- GitHub Actions 现在会从 `app/build.gradle.kts` 读取当前 `applicationId`，并自动同步到构建时生成的 `google-services.json`，避免包名变化后再次卡住自动构建。
- 延续上一版的 Zion 风格页面壳层和自动发版链路，新的 `0.2.0` 版本将作为后续继续重构的基础版本。

## v0.1.0

- 修复翻译页语言选择器在当前 Material3 版本下的下拉菜单兼容问题，恢复 GitHub Actions 的签名 APK 构建。
- 优化统一 Zion 风格头部的长标题截断与输入组件细节，减少 WebView、表单编辑页的视觉和布局风险。
- 延续 `v0.0.1` 的主题基线、页面壳层和自动发布链路改造，本版将作为新的自动发布版本。

## v0.0.1

- 全量切到 ZionChat 风格主题基线，统一浮动头部、字体、无 ripple 点击反馈、页内表单与分组卡片视觉。
- 把 rikkahub 现有页面的头部和主容器批量改造成 Zion 风格，覆盖聊天周边、设置链路、助手详情、备份、搜索、日志、统计、分享、翻译、WebView 等页面。
- 优化发布链路：版本线重置为 `0.0.1 (1)`，默认只构建签名 APK，Release 将自动附带 APK 资产。
- 优化 APK 体积与构建速度：Release 仅输出 `arm64-v8a` 与 `universal` APK，开启 Gradle 并行/缓存/配置缓存，移除 R8 的 `-dontobfuscate` 限制。

## v2.3.0

- 修复 GitHub Actions 自动签名构建的 keystore 路径解析问题，避免继续指向旧的 `app/app.key` 位置。
- 修复首批 Zion 改造里缺失的 Compose 引用与主题导入问题，消除设置页、历史页、助手页、提示词页的编译阻塞。
- 继续推进聊天页重构，已切换 Zion 风格的聊天头部、抽屉背景、会话列表和输入区基础视觉。

## v2.2.0

- 首批接入 ZionChat 风格基础层，统一新的浮动头部、按钮反馈、字体和分组卡片视觉。
- 设置页、历史页、收藏页、助手页、提示词页开始切换到新的 Zion 风格页面壳层。
- GitHub Actions 升级为推送即自动签名构建，并支持在缺少 Firebase secret 时使用占位配置继续出包。
